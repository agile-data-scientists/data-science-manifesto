module litchi.specs {
    'use strict';

    describe('Tabs module', () => {
        it('should have the listen method', () => {
           expect(litchi.Tabs.listen).toBeDefined();
        });
    });
}